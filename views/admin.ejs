<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/admin.css">
    <title>Admin Dashboard</title>
</head>
<body>
    <h1>Welcome to the Admin Dashboard</h1>
    <p>Welcome, Here you can manage your articles.</p>

    <main>
        <section id="controls">
            <a href="/new" class="button add-button">Add new Article</a>
        </section>

        <section id="articles-list">
            <h2>Published Articles</h2>
            <ul>
                <% articles.forEach(article => { %>

                    <li class="article-item">
                        <span class="article-title"><%= article.title %></span>
                        <span class="article-date"><%= article.date %></span>
                        <a href="/edit/<%= article.id %>" id="article-id" class="button edit-button">Edit</a> 
                        <button class="button delete-button" onclick="deleteArticle('<% article.id %>')">Delete</button> 
                    </li>
                <% }); %>
            </ul>
        </section>
    </main>
</body>
</html>